<ValidatedInput
    bind:value={((value))}
    label="((displayLabel))"
    validateAs={((validateAs))}
    type={((inputType))}
    bind:this={((thisBinder))}
    bind:keypress
    validationMessage="((validationMessage))"
    name="((name))"
    placeholder="((placeholder))" />